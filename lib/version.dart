/// The app's display version, shown as a small badge in the UI.
///
/// Bump this on each release (keep `pubspec.yaml`'s `version:` in sync). Uses a
/// baked-in constant so it renders instantly on web with no async lookup.
const String kAppVersion = '0.0.1';
